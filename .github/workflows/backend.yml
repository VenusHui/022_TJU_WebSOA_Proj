name: Deploy Backend

on:
  push:
    branches: [main]
    paths:
      - backendcloud/backend/**

jobs:
  deploy: Deployment
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
      - uses: actions/checkout@v2
        with:
          persist-credentials: false
